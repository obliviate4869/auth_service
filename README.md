## Setup
1. **Clone the repository:**
   ```bash
   git clone https://github.com/obliviate4869/auth_service.git
   cd auth_service
   ```
2. **Build and run the Docker containers:**
   ```bash
   docker-compose up
   ```
3. **Access the Django application:**
   go to http://127.0.0.1:8000/
